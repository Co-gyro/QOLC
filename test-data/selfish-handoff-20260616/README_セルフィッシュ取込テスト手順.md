# セルフィッシュ取込テスト 受け渡し資料（2026-06-16）

## 0. このテストの位置づけ（最初に共有）

- **目的**: JCB Link 形式の売上明細 → QOLC変換ツール → セルフィッシュ取込、の**フォーマット連携を検証**する。
- **注意**: 本データは **ダミー（テスト用）** です。6/11の実カード決済データではありません。
  実決済額との突合は、JCB/セゾン管理サイトに本番明細が反映され次第、同じ手順で別途実施します。
- 本日検証するのは「命名規則・列レイアウト・文字コード・マッチング」までです。

## 1. 渡すファイル（3点・Shift-JIS / CRLF）

| ファイル名 | 種別 | 内容 | 件数 |
|---|---|---|---|
| `JCB_UR_20260331_156742401.csv` | 売上明細(UR) | 取引明細（承認番号単位） | 16件 / 売上金額合計 ¥450,000（うち返金 -30,000 を1件含む） |
| `JCB_FI_20260331_156742401.csv` | 振込情報(FI) | 支払先・締め単位の振込サマリ | 4行（締め2回分） |
| `JCB_FM_20260331_156742401.csv` | 振込明細(FM) | 加盟店別の集計内訳 | 6行 |

※ これらは QOLC `/admin/csv-tools` の「JCB CSVリネーム」で、締日=2026/03/31・支払先番号=156742401 を指定して出力したものと**同一**です（リネームのみ・中身は無加工）。

## 2. マッチング用キー（セルフィッシュ検証環境マスタに登録ください）

| 項目 | 値 |
|---|---|
| 支払先番号 | `156742401` |
| 加盟店契約番号 | `241128445` |
| 加盟店番号（施設A） | `24112844500001`（テスト介護施設A） |
| 加盟店番号（施設B） | `24112844500002`（テスト介護施設B） |
| ブランド | JCB |
| 契約内容 | EC包括契約 |
| 締日（振込年月日） | `2026/03/31`（主）。**FI/FMには `2026/04/15` 分の行も混在** |

## 3. 当日の流れ

1. QOLC `/admin/csv-tools` で JCB_売上明細/振込情報/振込明細のダミー3点をアップロード
   → 種別が UR/FI/FM と自動判別されることを画面で確認
2. 締日 `2026-03-31`・支払先番号 `156742401` を入力 → 「まとめてZIPダウンロード」
3. 出力された3ファイルをセルフィッシュ検証環境に取込
4. 取込結果を確認：
   - [ ] ファイル名（命名規則）を認識して取り込めたか
   - [ ] UR/FI/FM の各列を正しくパースできたか
   - [ ] Shift-JIS が文字化けせず取り込めたか
   - [ ] 支払先番号→施設/加盟店のマッチングが効いたか
   - [ ] 金額集計がFI/FMのサマリと一致したか
5. 不備があればその場で列マッピング・フォーマットを調整

## 4. 既知の留意点（取込前に共有）

- **FI/FM は2締め分（03/31・04/15）が1ファイルに混在**しています。ファイル名は便宜上 `20260331` ですが、
  セルフィッシュ側で締め単位に分割が必要なら、振込年月日カラムでフィルタしてください。
- **返金行（売上金額がマイナス）** が UR・FI・FM に各1件含まれます（取消・返金の取込挙動確認用）。
- ダミーの列構成は実 JCB Link エクスポートを模したものです。**実明細反映後に列の完全一致を再確認**します。
